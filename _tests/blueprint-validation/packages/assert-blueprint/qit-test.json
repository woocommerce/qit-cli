{
	"package": "blueprint/assert",
	"test_type": "e2e",
	"description": "Fails unless the Blueprint state is already in place when the tests run.",
	"test": {
		"phases": {
			"run": [ "bash ./run.sh" ]
		},
		"results": {
			"ctrf-json": "./results/ctrf.json",
			"blob-dir": "./blob-report"
		}
	}
}
